function claimReward(rewardName) {
    // Here you can check if the user meets the requirements (e.g., completed challenges, points, etc.)
    // For simplicity, we're assuming the user can claim rewards immediately.

    alert(`Congratulations! You've claimed the ${rewardName}.`);

    // Update the user's profile or reward history (this can be done by adding logic to store rewards)
    // Example: Update a leaderboard or store the claimed reward in the database.
}
